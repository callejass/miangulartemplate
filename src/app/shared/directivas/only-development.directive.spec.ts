import { OnlyDevelopmentDirective } from './only-development.directive';

describe('OnlyDevelopmentDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyDevelopmentDirective();
    expect(directive).toBeTruthy();
  });
});
