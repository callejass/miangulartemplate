import { FormateoFechaPipe } from './formateo-fecha.pipe';

describe('FormateoFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new FormateoFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
